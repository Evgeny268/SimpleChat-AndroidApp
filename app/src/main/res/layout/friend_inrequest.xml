<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:padding="10dp"
    android:background="@color/colorPrimary"
    android:layout_width="match_parent"
    android:layout_height="130dp"
    android:orientation="horizontal">

    <TextView
        style="@style/TextAppearance.AppCompat.Large"
        android:id="@+id/inRequestNickname"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|start"
        android:layout_weight=".70"
        android:fontFamily="monospace"
        android:textColor="@color/textColors"
        android:textSize="18sp"
        android:textStyle="bold"
        tools:text="NoobMaster69"></TextView>
        <LinearLayout
            android:layout_width="0dp"
            android:layout_weight=".30"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|start"
            android:orientation="vertical">

            <Button
                android:id="@+id/acceptRequestIn"
                android:layout_marginVertical="5dp"
                android:layout_width="match_parent"
                android:background="@drawable/primary_dark_button"
                android:textColor="@color/textColors"
                android:layout_height="45dp"
                android:layout_gravity="center_vertical|end"
                android:text="@string/accept">

            </Button>

            <Button
                android:id="@+id/rejectRequestIn"
                android:layout_marginVertical="5dp"
                android:layout_width="match_parent"
                android:background="@drawable/border_button"
                android:textColor="@color/textColors"
                android:layout_height="45dp"
                android:layout_gravity="center_vertical|end"
                android:text="@string/reject"></Button>
        </LinearLayout>
    </LinearLayout>